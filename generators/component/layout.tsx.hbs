/**
 *
 * {{ properCase name }}
 *
 */

import * as React from "react";
import { Text, View } from "react-native";
import styles from "./styles";

type Props = {};

const {{ properCase name }}Layout = (_props: Props) => {
  return (
    <View style={styles.container}>
      <Text style={styles.header}>{{ titleCase name }}</Text>
    </View>
  );
};

export default {{ properCase name }}Layout;
