import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: 20
  },
  time: {
    alignSelf: "center",
  },
  backButton: {
    position: "absolute",
    top: 50,
    left: 20
  }
});

export default styles;
