/**
*
* Workout
*
*/

import * as React from "react";
import WorkoutLayout from "./workout.layout";

type Props = {};

const WorkoutLogic = (props: Props) => {
return (
<WorkoutLayout />
);
};

export default WorkoutLogic;