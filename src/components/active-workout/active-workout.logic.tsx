/**
*
* ActiveWorkout
*
*/

import * as React from "react";
import ActiveWorkoutLayout from "./active-workout.layout";

type Props = {};

const ActiveWorkoutLogic = (props: Props) => {
return (
<ActiveWorkoutLayout />
);
};

export default ActiveWorkoutLogic;