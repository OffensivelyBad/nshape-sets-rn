/**
*
* SliderInput
*
*/

import * as React from "react";
import SliderInputLayout from "./slider-input.layout";

type Props = {};

const SliderInputLogic = (props: Props) => {
return (
<SliderInputLayout />
);
};

export default SliderInputLogic;