/**
*
* Timer
*
*/

import * as React from "react";
import TimerLayout from "./timer.layout";

type Props = {};

const TimerLogic = (props: Props) => {
return (
<TimerLayout />
);
};

export default TimerLogic;